import random
import string

def gen_pass(pass_length):
    elements = string.ascii_letters + string.digits + string.punctuation
    password = ''.join(random.choice(elements) for i in range(pass_length))
    return password
